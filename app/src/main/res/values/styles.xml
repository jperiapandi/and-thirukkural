<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="commentaryByTextStyle">
        <item name="android:textSize">12dp</item>
        <item name="android:textColor">#7b000000</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="badge1">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:backgroundTint">@color/badge_color1</item>
        <item name="android:background">@drawable/rounded_corner</item>
    </style>

    <style name="badge2">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:backgroundTint">@color/badge_color2</item>
        <item name="android:background">@drawable/rounded_corner</item>
    </style>

    <style name="badge3">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:backgroundTint">@color/badge_color3</item>
        <item name="android:background">@drawable/rounded_corner</item>
    </style>

    <style name="badge4">
        <item name="android:textSize">10dp</item>
        <item name="android:textColor">#000000</item>
        <item name="android:backgroundTint">@color/badge_color4</item>
        <item name="android:background">@drawable/rounded_corner</item>
    </style>

</resources>
